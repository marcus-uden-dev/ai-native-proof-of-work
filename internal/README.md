# Internal

Status: Internal / Not recruiter-facing

This folder is for local source maps and source indexes.

Do not expose private local paths or raw project context in recruiter-facing files.

The real files below are gitignored by default:

```text
internal/LOCAL_SOURCE_MAP.md
internal/source-indexes/*.md
```

Use templates in `internal/templates/` to create local copies.
